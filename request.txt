SELECT NAME,POSITION
FROM EMPLOYEE
GROUP BY NAME,POSITION HAVING DEPARTMENT.ID = 'need department';



SELECT NAME 
FROM EMPLOYEE E 
WHERE E.SALARY = 2 * (
	SELECT AVG(SALARY) 
	FROM EMPLOYEE Ch
	WHERE Ch.CHIEF_ID = E.ID);